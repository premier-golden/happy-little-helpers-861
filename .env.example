# Public settings only. Copy to .env.local and fill in your project values.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
# Legacy anon alternative (use only one key variable):
# VITE_SUPABASE_ANON_KEY=

# Same public values for existing server-side auth handlers:
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
